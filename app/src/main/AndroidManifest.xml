<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alamin.c.islamicapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ParaWiseSuraShowerActivty"></activity>
        <activity android:name=".ParaWiseSuraListActivity" />
        <activity android:name=".SuraShowerActivity" />
        <activity android:name=".Surah_ListActivity" />
        <activity android:name=".LikeDataShower" />
        <activity android:name=".SubjectListActivity" />
        <activity android:name=".BlessingsShowerActivity" />
        <activity android:name=".HisnulMulsimActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>